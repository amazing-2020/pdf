#include <iostream>
int main()
{
    using namespace std;
    int length;
    cout << "Please enter the length of it(long): ";
    cin >> length;
    cout << length << " long = " << length * 220 << " ma." << endl;
    return 0;
}